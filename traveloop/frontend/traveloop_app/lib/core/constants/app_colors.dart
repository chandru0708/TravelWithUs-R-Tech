import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand - Teal
  static const primary = Color(0xFF1D9E75);        // Teal 400 - CTAs, active states, nav highlights
  static const primaryDark = Color(0xFF0F6E56);    // Teal 600 - hover/pressed states
  static const primaryLight = Color(0xFFE1F5EE);   // Teal 50 - backgrounds, chips, tags

  // Accent - Amber
  static const accent = Color(0xFFEF9F27);         // Amber 400 - badges, tags, special callouts
  static const accentDark = Color(0xFFBA7517);     // Amber 600 - pressed accent
  static const accentLight = Color(0xFFFAEEDA);    // Amber 50 - accent surface backgrounds

  // Neutrals
  static const textPrimary = Color(0xFF2C2C2A);    // Gray 900 - all body text
  static const textSecondary = Color(0xFF888780);  // Gray 400 - hints, placeholders
  static const surfaceSecondary = Color(0xFFF1EFE8); // Gray 50 - secondary surfaces

  // Base
  static const background = Color(0xFFFFFFFF);     // White - primary card/screen background
  static const border = Color(0xFFB4B2A9);         // Gray 200 - borders at 0.5px

  // Status
  static const success = Color(0xFF4CAF50);        // Success green
  static const danger = Color(0xFFE53935);         // Danger red
  static const info = Color(0xFF2196F3);           // Info blue
}